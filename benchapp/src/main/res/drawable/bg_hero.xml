<?xml version="1.0" encoding="utf-8"?>
<!-- Slightly lighter than a normal card and outlined in the accent, so the headline reads as the
     result rather than as the first of six equal sections. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/surface_raised" />
    <stroke android:width="1dp" android:color="@color/accent_dim" />
    <corners android:radius="14dp" />
</shape>
