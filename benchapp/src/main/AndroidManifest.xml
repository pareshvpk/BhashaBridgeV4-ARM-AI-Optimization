<?xml version="1.0" encoding="utf-8"?>
<!--
  No permissions at all. Everything measured comes from /proc, /sys, Debug and BatteryManager, all
  of which are readable by any app for its own process on a stock unrooted device. A benchmark that
  needed a permission to run would be a benchmark nobody runs.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/ic_menu_manage"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BenchApp">

        <activity
            android:name=".smoke.BenchActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Exporting the JSON report to another app (email, Drive, a file manager) without
             requesting storage permission. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.reports"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
